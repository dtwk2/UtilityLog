<UserControl x:Class="UtilityLog.View.LogDbView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UtilityLog.View"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:infra="clr-namespace:UtilityLog.View.Infrastructure"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="HighlightRow" TargetType="{x:Type DataGridRow}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Converter={x:Static infra:Converter.ModConverter}}" Value="True">
                    <Setter Property="Background" Value="GhostWhite" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <DataGrid  x:Name="DataGrid1"  AutoGenerateColumns="true" RowStyle="{StaticResource HighlightRow}">
        <i:Interaction.Behaviors>
            <infra:DataGridHideBrowsableFalseBehavior/>
            <infra:DataGridCustomDataTemplateBehavior/>
        </i:Interaction.Behaviors>
    </DataGrid>
</UserControl>
