<UserControl
    x:Class="Pcs.Hfrr.Log.View.LogGridView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:Utility.Log.View.Controls"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <DockPanel>
        <ToolBar DockPanel.Dock="Top" MinHeight="30">
            <TextBlock Text="Update Rate" VerticalAlignment="Center" FontSize="16" Margin="2"></TextBlock>
            <Slider Margin="2" Template="{StaticResource MaterialDesignTimedSlider}" 
                    x:Name="MainSlider" 
                    Width="100" VerticalAlignment="Center" Value="1" Minimum="0.1" Maximum="2"/>
        </ToolBar>
        <controls:LogGrid x:Name="MainLogGrid" Style="{StaticResource LogGrid2}"/>
    </DockPanel>
</UserControl>